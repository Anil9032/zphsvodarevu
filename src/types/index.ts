export interface Student {
  fullName: string;
  previousClass: string;
  presentClass: string;
  phoneNumber: string;
}

export interface GalleryImage {
  id: string;
  url: string;
  caption: string;
}